using System;
using System.Text.RegularExpressions;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Managers.Helpers;
using Accessors.DBModels;
using Managers.Models;

namespace UnitTests.ManagersTests
{
    [TestClass]
    public class AddressDataModelToAddressTest
    {
        [TestMethod]
        public void ValidAddressDataModelToAddress()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(Address, AddressHelper.AddressDataModelToAddress(addressDataModel));
            //Assert.AreEqual(Address, AddressHelper.AddressDataModelToAddress(addressDataModel));
            //Assert.AreEqual(Address, AddressHelper.AddressDataModelToAddress(addressDataModel));
        }

        [TestMethod]
        public void InvalidAddressDataModelToAddress()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(Address, AddressHelper.AddressDataModelToAddress(0));
            //Assert.AreEqual(Address, AddressHelper.AddressDataModelToAddress(1));
            //Assert.AreEqual(true, AddressHelper.AddressDataModelToAddress(2));
        }

        [TestMethod]
        public void NullAddressDataModelToAddress()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(NULL, AddressHelper.AddressDataModelToAddress(NULL));
            //Assert.AreEqual(NULL, AddressHelper.AddressDataModelToAddress(NULL));
            //Assert.AreEqual(NULL, AddressHelper.AddressDataModelToAddress(NULL));
        }
    }
    [TestClass]
    public class AddressToAddressDataModelTest
    {
        [TestMethod]
        public void ValidAddressToAddressDataModel()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(address));
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(address));
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(address));
        }

        [TestMethod]
        public void InvalidAddressToAddressDataModel()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(0));
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(1));
            //Assert.AreEqual(AddressDataModel, AddressHelper.AddressToAddressDataModel(2));
        }

        [TestMethod]
        public void NullAddressToAddressDataModel()
        {
            throw new NotImplementedException();
            //Assert.AreEqual(NULL, AddressHelper.AddressToAddressDataModel(NULL));
            //Assert.AreEqual(NULL, AddressHelper.AddressToAddressDataModel(NULL));
            //Assert.AreEqual(NULL, AddressHelper.AddressToAddressDataModel(NULL));
        }
    }
}